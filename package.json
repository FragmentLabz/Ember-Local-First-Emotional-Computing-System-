{
  "name": "ember-journal",
  "version": "2.0.0",
  "description": "Local-first encrypted journaling",
  "main": "electron/main.cjs",
  "type": "module",
  "scripts": {
    "setup-all": "npm install",
    "start": "vite build && electron .",
    "dev": "vite",
    "build": "vite build",
    "electron": "vite build && electron .",
    "preview": "vite preview"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "vite": "^5.4.0"
  }
}
